supported_raster_formats <- c(
  `SAGA` = ".sdat",
  `SAGA Compressed` = ".sg-grd-z",
  `GeoTIFF` = ".tif"
)

supported_vector_formats <- c(
 `ESRI Shapefile` = ".shp",
 `GeoPackage` = ".gpkg",
 `GeoJSON` = "geojson"
)
